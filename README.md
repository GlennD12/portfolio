
  # Portfolio Website with Modes

  This is a code bundle for Portfolio Website with Modes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  